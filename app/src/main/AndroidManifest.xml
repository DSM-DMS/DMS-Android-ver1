<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="teamdms.dms_kotlin">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/all_app_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/all_app_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Activity.SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity.SignInActivity" />
        <activity android:name=".Activity.MainActivity" />
        <activity
            android:name=".Activity.ApplyStudyActivity"
            android:theme="@style/SubTheme" />
        <activity android:name=".Activity.ApplyStayActivity" />
        <activity android:name=".Activity.SignUpActivity" />
        <activity android:name=".Activity.ChangePWActivity" />
        <activity android:name=".Activity.SurveyActivity" />
        <activity android:name=".Activity.NoticeListActivity" />
        <activity android:name=".Activity.NoticeDetailActivity" />

        <receiver android:name=".Widget.MealWidget">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="android.appwidget.action.APPWIDGET_DISABLED"/>
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_meal" />
        </receiver>

        <activity android:name=".Activity.IntroDeveloperActivity"></activity>
    </application>

</manifest>